<manifest xmlns:dist="http://schemas.android.com/apk/distribution"
    package="es.littledavity.dynamicfeatures.create">

    <dist:module
        dist:instant="false"
        dist:title="@string/feature_create">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="false" />
    </dist:module>
</manifest>

